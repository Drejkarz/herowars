{
  "name": "hw",
  "version": "1.0.0",
  "description": "HWA /mystats helper",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.11",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
